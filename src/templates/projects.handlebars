<section class="cv-component projects-component">
    <h2 class="section-title projects-title">Projects</h2>
    <ul class="no-bullets projects">
        {{#each projects}}
        <li class="bottom-dashed">
            <strong>{{title}}</strong>
            {{#each links}}
            <p>
                <a href="{{url}}" class="link hover-highlight" target="_blank" title="{{tooltip}}">{{url}}</a>
            </p>
            {{/each}}
            <p class="description">
                {{description}}
            </p>
        </li>
        {{/each}}
    </ul>
    <link rel="stylesheet" href="./styles/projects.css">
</section>